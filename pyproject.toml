[project]
name = "cleaner-rivers"
version = "0.3.0"
description = "A datapack that removes sand and gravel disks from rivers."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beet>=0.114.1",
]
